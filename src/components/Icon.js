import { createIconSetFromIcoMoon } from "react-native-vector-icons";
import icoMoonConfig from "../json/selection";

const Icon = createIconSetFromIcoMoon(icoMoonConfig, "icomoon", "icomoon.ttf");

export default Icon;
